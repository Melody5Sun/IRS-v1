{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "backend",
      "description": "FastAPI 后端（需先按 lessons.md 用 uv 建好 SystemCode/backend/.venv），接口文档在 /docs",
      "runtimeExecutable": "SystemCode/backend/.venv/Scripts/python.exe",
      "runtimeArgs": ["-m", "uvicorn", "app.main:app", "--app-dir", "SystemCode/backend", "--reload", "--reload-dir", "SystemCode/backend/app", "--port", "8000"],
      "port": 8000
    }
  ]
}

# 环境变量模板 — 复制为 .env 并填入真实值，.env 不会被提交
# 待架构确定后补充实际所需的变量（如 Neo4j 连接串等）

# LLM 供应商已选定为 Google Gemini 免费额度（无需信用卡，OpenAI 兼容接口，
# 代码零改动直接接入）。API Key 在 https://aistudio.google.com/apikey 申请
# LLM_API_KEY=
# LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/
# LLM_MODEL=gemini-3.6-flash

# JD 结构化处理可直接使用 Gemini 原生接口
# GEMINI_API_KEY=your_gemini_api_key_here
# GEMINI_MODEL=gemini-3.6-flash
# NEO4J_URI=
# NEO4J_USER=
# NEO4J_PASSWORD=

# 岗位职责语义匹配（本地模型，首次使用会下载并缓存）
# RESPONSIBILITY_EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
# RESPONSIBILITY_SIMILARITY_FLOOR=0.35
# RESPONSIBILITY_SIMILARITY_FULL=0.75
